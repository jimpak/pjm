spring:
  datasource:
    hikari.maximum-pool-size: 4
    url: jdbc:postgresql://localhost:5432/sns?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: pjm
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    database: postgresql
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true
  thymeleaf3:
    decoupled-logic: true